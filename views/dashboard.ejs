<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="/javascripts/script.js" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full h-full p-10 bg-zinc-900 text-white">
      <div class="w-4/5 flex justify-between items-center mb-4">
       <h1 class="text-2xl tracking-tighter">Your Dashboard</h1>
       <a href="/logout" class="inline-block px-8 py-3 border-2 border-transparent text-blue-500 underline rounded-lg" >Logout</a>
      </div>
      <div>
        <input class="px-2 py-3 border-2 border-zinc-600 outline-none rounded-lg " type="email" readonly placeholder="email" name="email" value="<%= user.email%>"/>
        <input class="px-2 py-3 border-2 border-zinc-600 outline-none rounded-lg " type="text" readonly placeholder="username" name="username" value="<%= user.username%>" />
        <input class="px-2 py-3 border-2 border-zinc-600 outline-none rounded-lg " type="text" readonly placeholder="name" name="name" value="<%= user.name%>" />
        <input class="px-2 py-3 border-2 border-zinc-600 outline-none rounded-lg " type="number" readonly placeholder="age" name="age" value="<%= user.age%>" />
      </div>
    </div>
  </body>
</html>
